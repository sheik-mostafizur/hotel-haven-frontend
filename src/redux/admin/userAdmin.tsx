import {createSlice} from "@reduxjs/toolkit";

const initialState = {};

export const userAdmin = createSlice({
  name: "adminUser",
  initialState,
  reducers: {},
});

export const {} = userAdmin.actions;

export default userAdmin.reducer;
